import pages from './pages';
import menu from './menu';

const exportConfigs = {
  id: 'settings',
  defaultPage: 'externalAccounts',
  title: 'settings',
  variant: 'indexMenu',
  menu,
  pages,
};

export default exportConfigs;
